rootProject.name = 'oc-da'

// Shortcut so module can be built without having a local repos (both project folders must reside in same folder)
def pluginProject = file("../opencms-plugin/")
if(pluginProject.exists()) {
    includeBuild pluginProject
}
